const nowPlaying = {
  async render() {
    return `
            <h2>Now Playing</h2>`;
  },

  async afterRender() {
    // Fungsi ini akan dipanggil setelah render()
  },
};

export default nowPlaying;
